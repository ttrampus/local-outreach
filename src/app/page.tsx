// Placeholder. Replaced by the real marketing page (hero → who we are → how we
// work → examples → pricing → book a call) before anything goes public.
export default function HomePage() {
  return (
    <main className="max-w-2xl mx-auto px-8 py-24">
      <h1 className="text-2xl font-semibold">Coming soon</h1>
    </main>
  );
}
